
services:
  # Ваш Node.js сервер
  app:
    build: .
    container_name: portfolio-app
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped